import { connect } from 'react-redux'
import modules from 'modules'
import GeoLocationPickerView from '../components/GeoLocationPickerView'

const mapDispatchToProps = (dispatch) => ({})

const mapStateToProps = (state) => ({})

export default connect(mapStateToProps, mapDispatchToProps)(GeoLocationPickerView)
