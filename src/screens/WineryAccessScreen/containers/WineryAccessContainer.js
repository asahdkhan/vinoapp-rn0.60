import {connect} from 'react-redux'
import WineryAccessView from '../components/WineryAccessView'

const mapDispatchToProps = (dispatch) => ({})

const mapStateToProps = (store) => ({})

export default connect(mapStateToProps, mapDispatchToProps)(WineryAccessView)
