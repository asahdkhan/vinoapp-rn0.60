import { connect } from 'react-redux'
import modules from 'modules'
import ListPickerView from '../components/ListPickerView'

const mapDispatchToProps = (dispatch) => ({})
const mapStateToProps = ({ locations, }) => ({})

export default connect(mapStateToProps, mapDispatchToProps)(ListPickerView)
